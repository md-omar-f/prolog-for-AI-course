circlearea :- read(R), A is 3.14 * R * R, write(A).
rectanglearea :- read(H), read(W), A is H * W, write(A).
trianglearea :- read(B), read(H), A is 1/2 * B * H, write(A).
cubearea :- read(S), A is S * S, write(A).

